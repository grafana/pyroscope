apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phlare.fullname" . }}-test-connection"
  labels:
    {{- include "phlare.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phlare.fullname" . }}:{{ .Values.phlare.service.port }}']
  restartPolicy: Never

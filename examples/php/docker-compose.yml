---
version: '3.9'
services:
  pyroscope:
    image: 'grafana/pyroscope:latest'
    ports:
      - '4040:4040'
    command:
      - 'server'
  app:
    platform: linux/amd64
    build: .
    cap_add:
      - SYS_PTRACE

receivers:
  otlp:
    protocols:
      grpc:

exporters:
  logging:

processors:
  batch:

extensions:
  health_check:
  pprof:
    endpoint: :6060

service:
  extensions: [pprof, health_check]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [logging]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [logging]

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pyroscope-monitoring.fullname" . }}-rules
  labels:
    {{- include "pyroscope-monitoring.labels" . | nindent 4 }}
{{- include "pyroscope-monitoring.rules-configmap" . }}

[package]
name = "rustdeps"
version = "0.3.3"
authors = ["Dmitry Filimonov"]

[lib]
crate-type = ["staticlib"]

[dependencies]
# py-spy = { path = "../../../py-spy" }
# rbspy = { path = "../../../rbspy" }

py-spy = { git = "https://github.com/pyroscope-io/py-spy", rev = "f990f0b" }
rbspy = { git = "https://github.com/pyroscope-io/rbspy", rev = "be059a6" }

[patch.crates-io]
benfred-read-process-memory = { git = 'https://github.com/pyroscope-io/read-process-memory.git', branch = 'benfred-read-process-memory'}
